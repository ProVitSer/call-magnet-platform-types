export enum Role {
    api = 'api',
    analitic = 'analitic',
    crm = 'crm',
    autoDial = 'auto-dial',
    sms = 'sms',
    telegram = 'telegram',
}

export enum Status {
    active = 'active',
    deactivated = 'deactivated',
}
