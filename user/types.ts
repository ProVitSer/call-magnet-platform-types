export enum Role {
    api = 'api',
    analitic = 'analitic',
    crm = 'crm',
    autoDial = 'auto-dial',
    sms = 'sms',
    telegram = 'telegram',
    kpi = 'kpi',
}

export enum Status {
    active = 'active',
    deactivated = 'deactivated',
}
